---
// Grid container for stat cards
interface Props {
  cols?: 2 | 3 | 4;
}

const { cols = 3 } = Astro.props;

const gridCols = {
  2: 'md:grid-cols-2',
  3: 'md:grid-cols-3',
  4: 'md:grid-cols-2 lg:grid-cols-4'
};
---

<div class={`grid gap-4 my-8 ${gridCols[cols]}`}>
  <slot />
</div>
